services:
  - type: web
    name: meu-catalogo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000  # Render sobrescreve automaticamente
      - key: FLASK_ENV
        value: production

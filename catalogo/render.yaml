services:
  - type: web
    name: meu-catalogo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000   # Render vai sobrescrever automaticamente
